# Pinglix frontend

Phase 2 provides the React and TypeScript frontend foundation and verifies the
connection to the Pinglix backend health endpoint.

```bash
npm install
npm run dev
```

The frontend runs at `http://localhost:5173`.
